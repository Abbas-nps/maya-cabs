# dependencies
node_modules/

# production build
dist/
dist-server/

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# environment secrets (VERY IMPORTANT)
.env
.env.local
.env.*.local
.env.migration         # service-role keys — never commit
.env.migrated          # generated env with pro URL — add anon key before committing
scripts/snapshots/     # raw data exports — may contain PII
# Local Netlify folder
.netlify
